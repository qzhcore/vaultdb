# vaultdb
vaultdb is a high-performance, concurrent key-value store written in Go. Featuring thread-safe memory storage via lock-striping and sequential data persistence through an append-only Write-Ahead Log (WAL), it provides absolute durability with minimal latency.
